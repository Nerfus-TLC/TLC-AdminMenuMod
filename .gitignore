# Third-party files and old work kept locally for reference only. Nothing in here is
# ours to redistribute, and it must never enter a public repository.
archive/

# Internal working notes. Useful while developing, noise to everyone else.
STATUS.md
docs/

# Build output. The archives are uploaded as release assets, not committed.
dist/
.staging/
